# estop_keyboard

Keyboard-driven E-STOP publisher for the ForzaETH race stack.

Usage (local run):

```bash
python3 controller/estop_keyboard/estop_keyboard/keyboard_estop.py
```

Or install the package in the workspace and run with `ros2 run estop_keyboard keyboard_estop`.
